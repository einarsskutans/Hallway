#include "velocity.h"

Velocity::Velocity() {}
Velocity::Velocity(int newtop, int newleft, int newright, int newbottom) {
    top = newtop;
    left = newleft;
    right = newright;
    bottom = newbottom;
}
